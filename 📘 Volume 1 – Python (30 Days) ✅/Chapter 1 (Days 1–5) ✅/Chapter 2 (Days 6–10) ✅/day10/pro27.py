# Program 3 – Username Display

# Take a username and print:

# Username : Akshata
# First Character : A
# Last Character : a
# Length : 7

username = "Akshata"
first_character = username[0]
last_character = username[-1]
length = len(username)

print(f"NAME : {username}\n"
      f"FIRST CHARACTER : {username[0]}\n"
      f"SECOND CHARACTER : {username[-1]}\n"
      f"LENGTH : {len(username)}\n")